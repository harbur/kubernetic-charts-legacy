apiVersion: v1
kind: Pod
metadata:
  name: {{ template "fullname" . }}-mountconfig
spec:
  restartPolicy: Never
  containers:
  - name: app
    image: "alpine:3.4"
    command: ["cat", "/config/app.conf"]
    volumeMounts:
    - mountPath: /config
      name: config
  volumes:
    - name: config
      configMap:
        name: {{ template "fullname" . }}
        items:
        - key: key-c
          path: app.conf
